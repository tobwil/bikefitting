# Bundled pose models

Run `npm install && npm run vendor:mediapipe` to copy WASM and download the
pinned Lite + Full Pose Landmarker `.task` files into this folder.

See `MANIFEST.md` for versions and SHA-256 hashes.
